<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sesion">
    <select id="loginColaborador" resultType="pojo.Colaborador">
    SELECT 
        colaborador.numeroPersonal, 
        colaborador.nombre, 
        colaborador.apellidoPaterno, 
        colaborador.apellidoMaterno,  
        colaborador.correoElectronico, 
        colaborador.sucursal, 
        colaborador.numeroLicencia, 
        colaborador.rol
    FROM vw_colaborador_activos colaborador 
    WHERE 
        colaborador.numeroPersonal = #{numPersonal} AND `password` = #{password}
    </select>


</mapper>

